kind = "web"
previewPath = "/"
title = "Global Digital Media Streaming Service"
version = "1.0.0"
id = "artifacts/streaming-site"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 19326

[services.development]
run = "pnpm --filter @workspace/streaming-site run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/streaming-site", "run", "build" ]
publicDir = "artifacts/streaming-site/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "19326"
BASE_PATH = "/"
